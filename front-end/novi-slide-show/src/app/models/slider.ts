export class Slider {
  id: number
  url : string;
  duration: number;

  constructor(id: number, url: string , duration: number, ) {
    this.id = id;
    this.url = url;
    this.duration = duration;
  }
}
